import { scenarios } from '../fixtures/scenarios.ts'; import { runScenario } from './scenario-runner.ts';
if(process.argv[2]!=='scenarios'){console.error('Usage: npm run scenarios');process.exitCode=2}else{const results=scenarios.map(runScenario);for(const r of results)console.log(`${r.pass?'PASS':'FAIL'} ${r.id}${r.failures.length?`: ${r.failures.join(', ')}`:''}`);const passed=results.filter(x=>x.pass).length;console.log(`\n${passed}/${results.length} deterministic scenarios passed`);if(passed!==results.length)process.exitCode=1;}
