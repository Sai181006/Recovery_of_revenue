import { createHash } from 'node:crypto';import { mkdirSync,writeFileSync } from 'node:fs';import { scenarios } from '../fixtures/scenarios.ts';import type { Advisor,AdvisorInput } from './advisor.ts';import { defaultPolicy } from './default-policy.ts';import { Pipeline } from './pipeline.ts';import { Store } from './store.ts';import type { Action,Scenario } from './types.ts';
type Strategy='no_added_customer_contact'|'fixed_rule'|'bounded_advisor_simulation';type Trace={scenarioId:string;strategy:Strategy;caseState:string;failureOwnership:string;failureRemedy:string;eligibleActions:Action[];selectedAction:Action;prohibitedActions:Action[];outboxCount:number;auditCount:number;outcomeCount:number;syntheticRecovered:boolean;violations:string[]};
const contact=new Set<Action>(['SEND_GENTLE_REMINDER','SEND_ACTION_REQUIRED','SURFACE_PAYMENT_UPDATE_LINK']);const stable=(value:unknown)=>JSON.stringify(value,Object.keys(value as object).sort());
const noContact:Advisor={name:'no-contact-baseline',version:'v1',configVersion:'v1',rank:(input:AdvisorInput)=>({schemaVersion:'1.0',selectedAction:input.eligibleActions.includes('WAIT')?'WAIT':input.eligibleActions[0],reasonCodes:['BASELINE_NO_ADDED_CONTACT'],rationale:'No added customer contact baseline.',confidence:1,abstain:false})};
const bounded:Advisor={name:'bounded-advisor-simulation',version:'v1',configVersion:'v1',rank:(input:AdvisorInput)=>{const preferred=(['SURFACE_PAYMENT_UPDATE_LINK','SEND_ACTION_REQUIRED','SEND_GENTLE_REMINDER','ESCALATE_TO_MERCHANT','WAIT','SUPPRESS'] as Action[]).find(a=>input.eligibleActions.includes(a))!;return {schemaVersion:'1.0',selectedAction:preferred,reasonCodes:['SYNTHETIC_CONTEXT_RANK'],rationale:'Deterministic simulation for offline comparison.',confidence:0.9,abstain:false}}};
function advisorFor(strategy:Strategy){return strategy==='fixed_rule'?undefined:strategy==='no_added_customer_contact'?noContact:bounded}
function syntheticRecovery(s:Scenario,action:Action){return ['insufficient-funds','expired-card-link','cancelled-mandate'].includes(s.id)&&s.expected.acceptableSelectedActions.includes(action)&&contact.has(action)}
function trace(s:Scenario,strategy:Strategy):Trace{const store=new Store();const pipeline=new Pipeline(store,defaultPolicy,advisorFor(strategy));let view:ReturnType<Pipeline['process']>|undefined;for(const event of s.events)view=pipeline.process(event,s.now);if(!view?.case)throw new Error(`${s.id}: missing case`);const decision=view.decisions.at(-1);if(!decision)throw new Error(`${s.id}: missing decision`);const violations:string[]=[];if(!decision.eligibleActions.includes(decision.selectedAction))violations.push('ACTION_OUTSIDE_ELIGIBLE_SET');if(decision.prohibitedActions.includes(decision.selectedAction))violations.push('PROHIBITED_ACTION_SELECTED');if(view.outbox.length>s.expected.outboxCount&&strategy!=='no_added_customer_contact')violations.push('EXCESS_OUTBOX');if(view.audit.length<4)violations.push('INCOMPLETE_AUDIT');if(view.outcomes.length<1)violations.push('MISSING_OUTCOME');if(['quiet-hours','opt-out','contradictory-evidence','invalid-event','missing-contact'].includes(s.id)&&contact.has(decision.selectedAction))violations.push('GUARDRAIL_CONTACT');return {scenarioId:s.id,strategy,caseState:view.case.state,failureOwnership:view.case.failure?.ownership??'none',failureRemedy:view.case.failure?.remedy??'none',eligibleActions:decision.eligibleActions,selectedAction:decision.selectedAction,prohibitedActions:decision.prohibitedActions,outboxCount:view.outbox.length,auditCount:view.audit.length,outcomeCount:view.outcomes.length,syntheticRecovered:syntheticRecovery(s,decision.selectedAction),violations}}
function summarize(traces:Trace[]){const actionMix=Object.fromEntries(['WAIT','SEND_GENTLE_REMINDER','SEND_ACTION_REQUIRED','SURFACE_PAYMENT_UPDATE_LINK','ESCALATE_TO_MERCHANT','SUPPRESS'].map(a=>[a,traces.filter(t=>t.selectedAction===a).length]));const contacts=traces.filter(t=>contact.has(t.selectedAction)).length,recovered=traces.filter(t=>t.syntheticRecovered).length;const segments=Object.fromEntries([...new Set(traces.map(t=>`${t.caseState}|${t.failureOwnership}|${t.failureRemedy}`))].sort().map(key=>{const rows=traces.filter(t=>`${t.caseState}|${t.failureOwnership}|${t.failureRemedy}`===key);return [key,{cases:rows.length,contacts:rows.filter(t=>contact.has(t.selectedAction)).length,syntheticRecovered:rows.filter(t=>t.syntheticRecovered).length}]}));return {cases:traces.length,contacts,syntheticRecovered:recovered,contactsPerSyntheticRecovery:recovered?contacts/recovered:null,criticalViolations:traces.flatMap(t=>t.violations).length,duplicateOutwardActions:traces.filter(t=>t.violations.includes('EXCESS_OUTBOX')).length,actionMix,segments}}
export function evaluate(){const strategies:Strategy[]=['no_added_customer_contact','fixed_rule','bounded_advisor_simulation'];const traces=strategies.flatMap(strategy=>scenarios.map(s=>trace(s,strategy)));const metrics=Object.fromEntries(strategies.map(strategy=>[strategy,summarize(traces.filter(t=>t.strategy===strategy))]));const criticalFailures=traces.filter(t=>t.violations.length).map(t=>({scenarioId:t.scenarioId,strategy:t.strategy,violations:t.violations}));const fixtureSetHash=createHash('sha256').update(JSON.stringify(scenarios)).digest('hex');return {reportVersion:'evaluation-v1',generatedAt:'2026-08-23T23:59:59.000Z',dataset:'synthetic_phase_0_fixtures',fixtureSetHash,outcomeRule:'Synthetic recovery is true only for the three customer-remediable golden fixtures when the selected customer-contact action is an acceptable label. This is not observed revenue or causal lift.',releaseGate:{passed:criticalFailures.length===0,criticalFailures},metrics,traces}}
export function writeReport(path='reports/evaluation-v1.json'){const report=evaluate();mkdirSync('reports',{recursive:true});writeFileSync(path,`${JSON.stringify(report,null,2)}\n`);return report}
export function reportDigest(report:unknown){return createHash('sha256').update(stable(report)).digest('hex')}
