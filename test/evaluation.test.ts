import test from 'node:test';import assert from 'node:assert/strict';import { evaluate,reportDigest } from '../src/evaluation.ts';
test('batch evaluation is deterministic and covers all strategies and fixtures',()=>{const first=evaluate(),second=evaluate();assert.equal(reportDigest(first),reportDigest(second));assert.equal(first.traces.length,42);assert.deepEqual(Object.keys(first.metrics),['no_added_customer_contact','fixed_rule','bounded_advisor_simulation'])});
test('critical release gate passes with zero prohibited, duplicate, audit, or guardrail failures',()=>{const report=evaluate();assert.equal(report.releaseGate.passed,true);assert.deepEqual(report.releaseGate.criticalFailures,[]);for(const metrics of Object.values(report.metrics)){assert.equal(metrics.criticalViolations,0);assert.equal(metrics.duplicateOutwardActions,0)}});
test('synthetic outcome claims remain explicitly labeled and baseline has no customer contact',()=>{const report=evaluate();assert.match(report.outcomeRule,/not observed revenue or causal lift/);assert.equal(report.metrics.no_added_customer_contact.contacts,0);assert.equal(report.metrics.no_added_customer_contact.syntheticRecovered,0)});
test('every trace links exact eligibility, decision, audit, outbox, and outcome evidence',()=>{for(const trace of evaluate().traces){assert.ok(trace.eligibleActions.includes(trace.selectedAction));assert.equal(trace.prohibitedActions.includes(trace.selectedAction),false);assert.ok(trace.auditCount>=4);assert.ok(trace.outcomeCount>=1)}});
